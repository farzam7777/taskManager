<div class="form-margin">
  <% @tasks.each do |task| %>
    
    <h2> 
      <%= link_to task.title.capitalize, task_path(task), :class => "link-text"%></span> 
    </h2>
    
    <p> <%= task.description %> </p>
    
    <hr>    
  <% end %>  
</div>
