<h3>Create Comment</h3>

<%= form_for [@task, @comment] do |f| %>
  
  <%= f.label :content, style: "font-size: 20px;" %><br>
  <%= f.text_area :content, size: "30x5", style: "margin-top: 1%" %><br>

  <%= f.submit "Create", class: "comment-submit-btn" %>

<% end %>  
